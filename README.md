# Smart Office Security — VS Code / Vercel

Static HTML/CSS/JavaScript conversion of the supplied PHP project.

Open `index.html` in VS Code (Live Server recommended). Login accepts any username/password for demo purposes and stores the username in localStorage.

This is frontend-only: there is no MySQL or server-side authentication.
